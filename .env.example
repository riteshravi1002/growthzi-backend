# MongoDB connection string (local or cloud)
MONGO_URI=mongodb://localhost:27017/growthzi

# Secret key used for signing JWT tokens (keep this safe in .env)
JWT_SECRET_KEY=your_jwt_secret_here

# OpenRouter (OpenAI-compatible) API key
OPENAI_API_KEY=your_openrouter_api_key_here
